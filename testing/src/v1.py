# In test driven development, you start by coding the test cases


class CRUD:
    # TODO after writing tests

    # return the item with the id (if any)
    def read(self, id: str) -> dict[str, str] | None:
        return None

    # return the overwritten object (if any)
    def put(self, item: dict[str, str]) -> dict[str, str] | None:
        return None

    # return the deleted item (if any)
    def delete(self, id: str) -> dict[str, str] | None:
        return None
